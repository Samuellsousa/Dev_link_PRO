name: CI

on:
  push:
    branches: [ "master" ]
  
jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]
        

    steps:
    - uses: actions/checkout@v3
    
    - name: install Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        
        
    - name: Install dependecies
      run: npm install
        
    - name: Build    
      run: yarn install build --if-present
    
    - name: yarn tests
      run: yarn test
      
    - name: Lint
      run: npx eslint src/**/*.ts
      
    - name: Run ts-prune  
      run: yarn install find-deadcode
